# Lean/Lake build artifacts.
/.lake/
# The public page in /site is a static file deployed as-is; nothing to ignore.
# A nested doc-gen4 project used to live in /docbuild and was removed — see
# docs/guide/open-work.md for why, and delete any leftover /docbuild locally.
/docbuild/

# Python validation artifacts.
__pycache__/
*.py[cod]

# Local agent / IDE tool caches and configuration (not product source).
.claude/
.codex/
.serena/
.tokensave/
.mcp.json
**/CLAUDE.md
.github/workflows/CLAUDE.md

# Accidental URL-as-path download debris (wget/curl without -O).
/https:/
/http:/

# Internal adversarial review rounds — process artifacts, kept local only.
/reviews/

# Ephemeral local scratch and tool dumps (not product source).
/scratchpad/
/ai_context.txt
/No_Free_Lunch_ML/
/SocialChoice/
/scl/
/.playwright-mcp/
